# My nix-darwin Configuration

```shell
sudo nix run nix-darwin/master#darwin-rebuild -- switch --flake .#default
# Once you've set up nix-darwin, you can use:
sudo darwin-rebuild switch --flake .#default
```

